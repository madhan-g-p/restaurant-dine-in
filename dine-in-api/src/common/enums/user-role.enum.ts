export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
  // Future RBAC roles — add here without modifying guards or schema logic:
  // KITCHEN_STAFF = 'KITCHEN_STAFF',
  // CASHIER = 'CASHIER',
  // MANAGER = 'MANAGER',
}
